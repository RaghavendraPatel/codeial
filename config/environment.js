const deployment = {
    name : 'development',
    asset_path : './assets',
    session_cookie_key : 'abcd',
    db : 'codeial'
}

const production = {
    name : 'producrion'
}

module.exports = deployment;